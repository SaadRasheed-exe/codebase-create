"""Codebase Create: an LLM coding agent that generates, tests, and refines Python code.

The package is being restructured toward an agentic architecture
(tool-calling loop, pluggable providers). Legacy pipeline modules live
alongside the new building blocks until the migration completes.
"""

__version__ = "0.1.0"
